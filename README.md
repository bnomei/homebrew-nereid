# homebrew-nereid

Homebrew tap for nereid.

## Install

```bash
brew install bnomei/nereid/nereid
```

## Maintain

Bump `version` and update all `sha256` values in `Formula/nereid.rb` to match the
GitHub Release tarballs from `bnomei/nereid`.

```bash
scripts/update-sha256.sh
```
